describe("Übung 2", () => {
  it("SHOULD create a successful pay transaction", () => {
    /**
     * TEST 1
     * Erstelle einen Test, der eine erfolgreiche Zahlung durchführt.
     * Überlege dir eine passende Überprüfung / Assertion am Ende des Tests.
     */
  });

  it("SHOULD create a successful request transaction", () => {
    /**
     * TEST 2
     * Erstelle einen Test, der eine erfolgreiche Transaction durchführt.
     * Überlege dir eine passende Überprüfung / Assertion am Ende des Tests.
     */
  });

  /**
   * Welche Schritte in den beiden Tests sind identisch?
   * Nutze die BeforEach() Methode um die Tests zu verbessern.
   *
   * Gibt es auch ein Schritte die nach einem Tests wichtig wären?
   * Nutze die AfterEach() Methode um die Tests zu verbessern.
   */
});
