describe("Übung 3", () => {
  beforeEach(() => {
    /**
     * Erstelle eine Custom Command Login, die einen Usernamen und Passwort erhält.
     * Benutze sie hier.
     */
  });

  it("SHOULD ", () => {
    /**
     * TEST 1
     * Erstelle einen Test ...
     * Überlege dir eine passende Überprüfung / Assertion am Ende des Tests.
     */
  });

  /**
   * Ändere die Custom Command Login dahin dass das Login per POST Request stattfindent.
   */
});
